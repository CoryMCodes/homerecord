<% content_for :title, "Homes" %>

<section>
  <h1>Homes</h1>

  <% if @homes.any? %>
    <ul>
      <% @homes.each do |home| %>
        <li><%= link_to home.name, home_path(home) %></li>
      <% end %>
    </ul>
  <% else %>
    <p>No homes yet.</p>
  <% end %>
</section>
