<dl class="mt-3 flex flex-wrap items-center gap-x-4 gap-y-1.5 text-sm text-muted-foreground">
  <% items.each do |item| %>
    <div class="inline-flex items-center gap-1.5">
      <dt class="sr-only"><%= item[:label] %></dt>
      <dd><%= item[:value] %></dd>
    </div>
  <% end %>
</dl>
